# URL base del API de Vikunja, utilizada por el frontend para todas las peticiones.
# Debe apuntar al dominio o subdominio por el que vas a acceder (el mismo que usas en NPM, más la ruta de la API).
VIKUNJA_API_URL=https://vikunja.lospajaros18.top/api/v1

# URL base del frontend de Vikunja (web principal)
# Indica la URL pública que usarán los usuarios para acceder a la app web (sin /api/v1)
VIKUNJA_SERVICE_FRONTENDURL=https://vikunja.lospajaros18.top

# --- Configuración de base de datos ---
# Contraseña del usuario 'vikunja' en la base de datos (define una contraseña segura)
MYSQL_PASSWORD=pon_aqui_tu_contraseña_segura

# Contraseña del usuario root de la base de datos (también debe ser segura, y distinta si lo prefieres)
MYSQL_ROOT_PASSWORD=pon_aqui_tu_contraseña_segura

# --- Clave secreta para JWT ---
# Cadena larga y aleatoria, usada para firmar los tokens de sesión (imprescindible que sea secreta)
VIKUNJA_SERVICE_JWTSECRET=pon_aqui_un_valor_largo_y_unico

