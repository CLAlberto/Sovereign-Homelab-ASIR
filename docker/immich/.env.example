# Rellena estos campos y renombra este archivo a ".env" para usarlo en tu entorno real
# Puedes encontrar la documentación de todas las variables soportadas en https://immich.app/docs/install/environment-variables

# Carpeta donde se guardarán tus fotos y vídeos subidos
UPLOAD_LOCATION=../../library/immich_data

# Carpeta donde se guardarán los datos de la base de datos Postgres (¡no usar carpetas de red tipo SMB/NFS para esto!)
DB_DATA_LOCATION=./postgres

# Para establecer la zona horaria, descomenta la siguiente línea y cambia Etc/UTC por tu zona horaria (ej: Europe/Madrid)
TZ=Europe/Madrid

# Versión de Immich a utilizar. Puedes fijar una versión concreta (ej: "v1.71.0") o dejarlo como "release" para usar siempre la última estable.
IMMICH_VERSION=release

# Contraseña para la base de datos Postgres. ¡Debes cambiarla por una contraseña fuerte! 
# Usa solo letras y números, sin espacios ni caracteres especiales.
DB_PASSWORD=XXXXXXXXXXXX

# Los valores que hay debajo de esta línea NO es necesario cambiarlos habitualmente.
###################################################################################

# Usuario de la base de datos Postgres (por defecto "postgres")
DB_USERNAME=postgres

# Nombre de la base de datos Postgres que usará Immich (por defecto "immich")
DB_DATABASE_NAME=immich

